namespace FogBugzAPI.Model.Cases.Fields {
    public enum FieldType
    {
        Boolean,
        StringList,
        String,
        IntegerList,
        Integer,
        DateTime,
        Double
    }
}